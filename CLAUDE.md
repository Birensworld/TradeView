# Working agreements

- Before pushing any commit to a remote branch, summarize the change and get
  explicit user confirmation first. This applies to every session working in
  this repo, not just the one where it was requested.
